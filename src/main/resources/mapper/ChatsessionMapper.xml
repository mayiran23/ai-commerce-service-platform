<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mayiran.commerceservice.mapper.ChatSessionMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_chat_session(user_id, session_id, create_time, update_time)
        values (#{userId}, #{sessionId}, #{createTime}, #{updateTime})
    </insert>
    <select id="getBySessionId" resultType="com.mayiran.commerceservice.entity.ChatSession">
        SELECT id, user_id, session_id, create_time, update_time
        FROM t_chat_session
        WHERE session_id = #{sessionId}
    </select>
</mapper>